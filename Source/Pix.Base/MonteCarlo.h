#pragma once

#include "Vector3.h"

namespace Pix::Base
{
    class MonteCarlo
    {
    public:
        //static void CreateLocalCoordinateSystem(const Vector3& normal, Vector3* normalTangent, Vector3* normalBitangent)

        //static UniformSampleHemisphere(float random1, float random2)
        //{
        //    float sinTheta = sqrtf(1 - r1 * r1);
        //    float phi = 2 * M_PI * r2;
        //    float x = sinTheta * cosf(phi);
        //    float z = sinTheta * sinf(phi);
        //
        //    return Vec3f(x, r1, z);
        //}
    };
}